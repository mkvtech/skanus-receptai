<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Pagrindinis</title>

  <link rel="stylesheet" href="/css/reset.css">
  <link rel="stylesheet" href="/css/common.css">

  <link rel="stylesheet" href="/css/pages/recipes/show.css">
</head>
<body>
  <%- include('../../partials/navbar.html.ejs', { context }); %>

  <div class="main-container">
    <header class="page-header">
      <h1 class="page-title"><%= recipe.title %></h1>

      <p class="recipe-author"><%= recipe.user.getFullName() %></p>
    </header>

    <p class="recipe-description">
      <%= recipe.description %>
    </p>

    <div class="recipe-bottom">
      <div class="recipe-rating">
        <div id="rater" data-initial-rating="<%= totalRating %>"></div>

        <% if(myRating) { %>
          <div class="recipe-current-user-rating">(Jūsų įvertinimas: <%= myRating %>)</div>
        <% } %>
      </div>
      <!-- TODO: Count comments, rating, etc. -->
    </div>

    <!-- TODO: Comments -->
  </div>

  <script src="/packages/rater-js/index.js"></script>
  <script src="/js/pages/recipes/show.js"></script>
</body>
</html>
